AC HRA Portal v27 — approval reconciliation and manpower approval detail

Approval workflow
- Reconciles the server approval ledger back into old open batches before anomaly reports.
- Approved items no longer appear as overdue, piled-up, or data anomalies.
- A batch with all decisions completed is automatically closed.
- The original Telegram approval message is updated with an approved/completed label.
- Completion creates online, Excel, and PDF report links when Drive permissions allow it.
- Approval status responses now include the report links and reconciled item states.
- Monthly completeness tracking now includes manpower approval.

Manpower
- Added a Manpower Approval tab with pending, approved, and rejected filters.
- Supports selecting requests by period, department, section, and position.
- Supports submit-for-approval, local status marking, server decision sync, and automatic cloud upload.
- Manpower approval and summary details include required, actual, gap, hired, resigned, ending, and remarks.
- Telegram approval results include department / section / position progress instead of only totals.

Preserved from v26
- Smart incremental sync: unchanged buckets are not uploaded or downloaded again.
- Cloud-only history is retained; shorter local snapshots do not overwrite longer cloud data.
- Existing Attendance date-shard sync, Telegram settings, approval ledger, webhook, and schedules remain compatible.

Deployment
- Replace the complete `ga_attendance_cloud.gs` and deploy a new version to the existing Web App URL.
- Replace the HRA Portal HTML/shared files from the ZIP.
- Do not run `一鍵安裝()` or `setupWebhook()` when the existing Bot, group, webhook, and schedules already work.

Verification
- GAS JavaScript syntax check passed.
- All 10 HRA HTML files passed inline JavaScript syntax checks.
- ZIP contents checked after packaging.
